{
  "name": "chatroom-ensemble",
  "version": "1.0.0",
  "description": "OpenCode Plugin for Chatroom Ensemble Roleplay",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --platform=node --outfile=dist/index.js",
    "build:tsc": "tsc",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@opencode-ai/plugin": "*",
    "@opencode-ai/sdk": "*",
    "phoenix": "^1.7.14",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/phoenix": "^1.6.7",
    "@types/ws": "^8.5.10",
    "esbuild": "^0.28.0",
    "typescript": "^5.5.4"
  }
}
